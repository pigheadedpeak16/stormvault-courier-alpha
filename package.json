{
  "name": "stormvault-courier-alpha",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:orb-balance": "node --experimental-strip-types src/game/core/orbBalance.spec.ts",
    "check:progression-balance": "node --experimental-strip-types src/game/core/progressionBalance.spec.ts",
    "build": "npm run check:orb-balance && npm run check:progression-balance && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
